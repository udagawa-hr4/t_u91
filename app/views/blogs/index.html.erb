
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <% content_for :css do %>
    <%= stylesheet_link_tag 'blogs' %>
  <% end %>
</head>
<body>
  
<div class="calendar-all">
  <nav class="top-nav">
    <%= link_to  root_path, class: "top-nav_left" do%>
    <span class="char-sub">T</span><span class="char-sub">o</span><span class="char-sub">p</span><span class="char-sub">&nbsp</span><span class="char-sub">P</span><span class="char-sub">a</span><span class="char-sub">g</span><span class="char-sub">e</span>
    <% end %>
    <%= link_to  new_blog_path, class: "top-nav_right" do%><span class="char-sub">M</span><span class="char-sub">y</span><span class="char-sub">&nbsp</span><span class="char-sub">P</span><span class="char-sub">a</span><span class="char-sub">g</span><span class="char-sub">e</span>
    <% end  %>
  </nav>

  <h1 class="calendar-header">My Diary</h1>

  <table></table>
  
  <%= link_to new_blog_path, class:"diary-nik" do %>
    <p class="nik-span">Write a diary</p>

   <% end %> 
  <%# <div class="calendar-index"> %>
    <%= month_calendar class:"calendar-index", events: @blogs do |date, blogs| %>
      <%= date.day %>

      <% blogs.each do |blog| %>
        <div>
          <%= link_to blog.title, blog %>
        </div>
      <% end %>
    <% end %>
  <%# </div> %>
</div>
<%= javascript_pack_tag "calendar.js"%>
</body>
</html>